language: python

python:
    - "3.4"
    - "3.5"
    - "3.6"
    - "3.7"
    - "pypy"
    - "pypy3"

install:
    - pip install --use-mirrors -r requirements.txt
    - python setup.py install

script:
    - pytest -rs tests
